import React from 'react';
// тут можно подключить css для менюхи
import MainMenuList from "../containers/MainMenuList";

const MainMenu = () => (
    <div>
        <p>This is menu items</p>
        <MainMenuList/>
    </div>
);

export default MainMenu;